# Copy this file to .env and fill in the proxy URL if needed
DEEZER_PROXY_URL=

# Google OAuth web client ID — required for Sign in with Google + hidden Drive sync.
# Create in Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs (Web).
GOOGLE_WEB_CLIENT_ID=
